import React from 'react';
import { View } from 'react-native';

export const BlurView = ({ children, style }) => {
  return <View style={style}>{children}</View>;
};
export const VibrancyView = ({ children, style }) => {
  return <View style={style}>{children}</View>;
};
